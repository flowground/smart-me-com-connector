{
    "type": "object",
    "properties": {
        "folderId": {
            "required": true,
            "type": "string"
        },
        "startDate": {
            "required": true,
            "type": "string",
            "format": "date-time"
        },
        "endDate": {
            "required": true,
            "type": "string",
            "format": "date-time"
        }
    }
}